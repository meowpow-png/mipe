name: CI

on:
  workflow_dispatch:
  push:
    branches: [ master, dev ]

permissions:
  contents: read
  packages: write

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          cache: true
          go-version-file: runtime/go.mod
          cache-dependency-path: runtime/go.sum

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Run tests
        working-directory: runtime
        run: go test -v ./...

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker images
        uses: docker/bake-action@v7
        with:
          source: ./runtime
          targets: |
            runtime
            node-base
            java-base
            web-base
          provenance: false
          sbom: false
          set: |
            runtime.cache-from=type=registry,ref=ghcr.io/${{ github.repository }}:runtime-buildcache
            runtime.cache-to=type=registry,ref=ghcr.io/${{ github.repository }}:runtime-buildcache,mode=max
          
            node-base.cache-from=type=registry,ref=ghcr.io/${{ github.repository }}:node-base-buildcache
            node-base.cache-from=type=registry,ref=ghcr.io/${{ github.repository }}:runtime-buildcache
            node-base.cache-to=type=registry,ref=ghcr.io/${{ github.repository }}:node-base-buildcache,mode=max
          
            java-base.cache-from=type=registry,ref=ghcr.io/${{ github.repository }}:java-base-buildcache
            java-base.cache-from=type=registry,ref=ghcr.io/${{ github.repository }}:node-base-buildcache
            java-base.cache-from=type=registry,ref=ghcr.io/${{ github.repository }}:runtime-buildcache
            java-base.cache-to=type=registry,ref=ghcr.io/${{ github.repository }}:java-base-buildcache,mode=max
          
            web-base.cache-from=type=registry,ref=ghcr.io/${{ github.repository }}:web-base-buildcache
            web-base.cache-from=type=registry,ref=ghcr.io/${{ github.repository }}:node-base-buildcache
            web-base.cache-from=type=registry,ref=ghcr.io/${{ github.repository }}:runtime-buildcache
            web-base.cache-to=type=registry,ref=ghcr.io/${{ github.repository }}:web-base-buildcache,mode=max
