# Architecture

## Structure

The repository is organized into two top-level directories:

```text
.
├── runtime/
└── examples/
```

### `runtime/`

Contains runtime implementation, including container 
entrypoints, initialization scripts, shared configuration, and runtime hooks.

### `examples/`

Contains example projects that demonstrate how the project is intended to be used.
